name: clean_forge
description: A powerful CLI tool for generating production-ready Clean Architecture features with comprehensive testing, DI, and multiple state management options
version: 0.0.1
repository: https://github.com/ishe19/clean_forge

environment:
  sdk: ^3.9.0

dependencies:
  path: ^1.9.0
  args: ^2.4.0
  mason_logger: ^0.3.0
  interact: ^2.2.0
  yaml: ^3.1.2
  io: ^1.0.4
  collection: ^1.18.0
  cli_util: ^0.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  mocktail: ^1.0.0

executables:
  clean_forge: